apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: cluster-dashboard
    files:
      - cluster.json
  - name: node-dashboard
    files:
      - node.json
generatorOptions:
  disableNameSuffixHash: true