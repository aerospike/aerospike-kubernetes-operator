# resources:
# - manager.yaml

# generatorOptions:
#   disableNameSuffixHash: true

# configMapGenerator:
# - name: manager-config
#   files:
#   - controller_manager_config.yaml


resources:
- manager.yaml
- service_account.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: sud82/test-operator
  newTag: v0.0.12
